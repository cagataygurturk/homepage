name: Build and Push Docker Image to GHCR

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-24.04-arm

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    env:
      REGISTRY: ${{ vars.CLOUDFLEET_ORGANIZATION_ID }}.europe.registry.cloudfleet.dev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ vars.CLOUDFLEET_API_KEY }}
          password: ${{ secrets.CLOUDFLEET_API_SECRET }}

      - name: Define image name and tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=,suffix=,format=long
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - uses: imranismail/setup-kustomize@v2

      - run: kustomize edit set image ghcr.io/cagataygurturk/homepage=${{ env.REGISTRY }}/${{ github.repository }}:${GITHUB_SHA}
        working-directory: kubernetes/homepage/

      - uses: azure/setup-kubectl@v5
        name: Setup kubectl

      - uses: cloudfleet-actions/configure-kubectl@v1
        name: Configure kubectl
        with:
          cluster-id: ${{ vars.CFKE_CLUSTER_ID }}
          region: ${{ vars.CFKE_REGION }}

      - run: kubectl auth whoami

      - run: kubectl apply -k .
        working-directory: kubernetes/cilium

      - run: kubectl apply -k .
        working-directory: kubernetes/homepage