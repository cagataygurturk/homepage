apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied manually by a cluster admin; the GitHub Actions identity cannot grant itself these permissions.
resources:
  - github-actions.yaml
